name: Setup Go
description: Setup golang
runs:
  using: composite
  steps:
    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        check-latest: true
        go-version-file: go.mod
